version: '3.8'

services:
  n8n:
    image: n8nio/n8n
    container_name: n8n-container
    restart: always
    ports:
      - "5678:5678"
    environment:
      - N8N_BASIC_AUTH_ACTIVE=true
      - N8N_BASIC_AUTH_USER=stevor94@gmail.com
      - N8N_BASIC_AUTH_PASSWORD=@8ixNaHiF82qWiW
      - N8N_LICENSE_KEY=0646207a-4503-4253-a99d-05593ba3a56b
      - N8N_RUNNERS_ENABLED=true
      - WEBHOOK_URL=https://generous-wren-fresh.ngrok-free.app/
      - N8N_PORT=5678
      - N8N_PROTOCOL=https
      - N8N_CORS_ALLOWED_ORIGINS=https://*.googleusercontent.com 
    volumes:
      - n8n_data:/home/node/.n8n

volumes:
  n8n_data:
    external: true
    name: Volumen1
